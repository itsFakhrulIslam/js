/**
 *^ recap previous all topics
 ** 1. variable (const/constant, let)
 ** 2. data type
        1. primitive type
            1. number (integer, float)
            2. string
            3. boolean
            4. undefined
            5. null
        2. non primitive type
            1. array
            2. object
            3. function
 ** 3. conditional
        1. comparison ( >, <, =, >=, <=, ==/===, !=/!==, &&, ||, ! )
        2. if, if/else, if/ifelse/else
 ** 4. loop
        1. for
        2. while
        3. for in
        4. for of
        5. break
        6. continue
 ** 5. array
        1. initialization [ , , , ]
        2. index
        3. get value array[indexNumber]
        4. set new value array[indexNumber] = newValue
        5. array.reverse()
        6. array.sort()
        7. array.push()
        8. array.pop()
        9. array.shift()
        10. array.unshift()
        11. array.slice()
        12. array.splice()
        13. array.includes()
        14. array.join()
        15. array.concat()
        16. for of loop
 ** 6. object
        1. initialization { key: value }
        2. access to object
            dot notation, object.key
            bracket notation, object['key']
        3. set new value object['key']/object.key = newValue
        4. for in loop
 ** 7. function
        1. init function fName(){}
        2. call function fName()
        3. pass the params by argument
        4. return the final output inside the function
 */